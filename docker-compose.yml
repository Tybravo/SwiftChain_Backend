version: '3.8'

services:
  app:
    build: .
    ports:
      - '5000:5000'
    environment:
      - PORT=5000
      - MONGO_URI=mongodb://mongo:27017/swiftchain
      - REDIS_URL=redis://redis:6379
      - NODE_ENV=development
    depends_on:
      - mongo
      - redis
    networks:
      - swiftchain-network

  redis:
    image: redis:7-alpine
    ports:
      - '6379:6379'
    networks:
      - swiftchain-network

  mongo:
    image: mongo:latest
    ports:
      - '27017:27017'
    volumes:
      - mongo-data:/data/db
    networks:
      - swiftchain-network

networks:
  swiftchain-network:
    driver: bridge

volumes:
  mongo-data:
